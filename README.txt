Name: Gizem Dal

The setup instructions are included in the report.

If you want to export each frame as OBJ, comment out the code in the frame loop and create a 'frames' directory in the same place where you have the py script.